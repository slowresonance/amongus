add_split -> add the current split to the splits array
add_user -> add the current user to the users array
add_contact -> add the friend to the user's contacts array
add_group -> add the group to the user's groups array

get_group_by_id -> get the group from the groups array
get_user_by_id -> get the user from the users array
get_split -> get the split from the splits array

